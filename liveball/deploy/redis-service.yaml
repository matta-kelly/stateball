apiVersion: v1
kind: Service
metadata:
  name: liveball-redis
  namespace: stateball
  labels:
    app: liveball-redis
spec:
  selector:
    app: liveball-redis
  ports:
    - port: 6379
      targetPort: 6379
      name: redis
  clusterIP: None
